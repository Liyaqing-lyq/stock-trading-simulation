# 02 系统架构设计

## 1. 分层与包结构

```
gui / app            表现层：JavaFX 界面、控制台演示
  └─ sim             装配层：SimulationRunner 组装引擎/账户/agent/定时器，驱动交易周期
       └─ engine     业务层：MatchingEngine（撮合）、OrderBook（订单簿）
          ├─ account 业务层：Account / AccountManager / PerformanceTracker
          ├─ market  业务层：MarketDataPublisher / MarketDataListener（行情广播）
          └─ agent   业务层：TradingAgent 及实现（人类、AI/LLM、噪音做市）
               └─ io / config / model   数据层：CSV 落盘、配置加载、不可变领域对象
```

依赖方向单向向下：`gui/app → sim → engine|account|market|agent → model`。
agent 只依赖 `OrderGateway` 接口，不接触引擎内部结构，便于替换与测试。

## 2. 类图

```mermaid
classDiagram
    class OrderGateway {
        <<interface>>
        +submit(OrderRequest) OrderResult
        +cancel(long) boolean
    }
    class MatchingEngine {
        -Object lock
        -Map~String,OrderBook~ books
        -AccountManager accounts
        -MarketDataPublisher publisher
        -AtomicLong orderIdSeq
        +submit(OrderRequest) OrderResult
        +cancel(long) boolean
        +closeMarket() int
        +snapshot(String,int) OrderBookSnapshot
        +marketSnapshot(String,int) MarketSnapshot
    }
    class OrderBook {
        -NavigableMap~BigDecimal,Deque~ bids
        -NavigableMap~BigDecimal,Deque~ asks
        -Map~Long,Order~ restingOrders
        -BigDecimal lastPrice
        +post(Order)
        +bestOpposite(Side) Order
        +remove(Order)
        +snapshot(int) OrderBookSnapshot
    }
    class AccountManager {
        -Map~String,Account~ accounts
        +createAccount(String,String,BigDecimal) Account
        +ranking(Map) List~PerformanceRank~
        +markAllEquity(Map)
    }
    class Account {
        -BigDecimal cash
        -BigDecimal frozenCash
        -Map~String,Position~ positions
        -Map~Long,Reservation~ reservations
        +reserve(long,String,Side,BigDecimal,long) boolean
        +settleBuy(long,String,BigDecimal,long)
        +settleSell(long,String,BigDecimal,long) BigDecimal
        +release(long)
        +equity(Map) BigDecimal
        +returnRate(Map) BigDecimal
        +seedPosition(String,long,BigDecimal)
    }
    class Position {
        -long quantity
        -long frozenQuantity
        -BigDecimal averageCost
        +addShares(long,BigDecimal)
        +removeShares(long,BigDecimal) BigDecimal
    }
    class MarketDataPublisher {
        -List~MarketDataListener~ listeners
        +subscribe(MarketDataListener)
        +publishTrade(Trade)
        +publishSnapshot(MarketSnapshot)
    }
    class TradingAgent {
        <<interface>>
        +onMarketSnapshot(MarketSnapshot)
        +onTick()
    }
    class AbstractTradingAgent {
        <<abstract>>
        #OrderGateway gateway
        #Account account
        +submit(OrderRequest) OrderResult
    }
    class HumanBrokerAgent
    class NoiseAgent
    class MomentumAgent
    class LlmTradingAgent {
        -LlmClient client
        -BigDecimal priceBandPercent
    }
    class LlmClient {
        <<interface>>
        +complete(String,String) String
    }
    class DeepSeekClient
    class DecisionParser {
        <<utility>>
        +parse(String) TradingDecision
    }
    class CsvHistoryRecorder {
        +onOrderAccepted(Order,List)
        +onTrade(Trade)
        +writeRanking(List)
    }
    class SimulationRunner {
        +start()
        +closeAndRank() List~PerformanceRank~
    }

    OrderGateway <|.. MatchingEngine
    MatchingEngine o-- OrderBook
    MatchingEngine --> AccountManager
    MatchingEngine --> MarketDataPublisher
    AccountManager o-- Account
    Account o-- Position
    MarketDataPublisher o-- MarketDataListener
    MarketDataListener <|.. AbstractTradingAgent
    TradingAgent <|.. AbstractTradingAgent
    AbstractTradingAgent <|-- HumanBrokerAgent
    AbstractTradingAgent <|-- NoiseAgent
    AbstractTradingAgent <|-- MomentumAgent
    AbstractTradingAgent <|-- LlmTradingAgent
    LlmTradingAgent --> LlmClient
    LlmClient <|.. DeepSeekClient
    LlmTradingAgent --> DecisionParser
    MarketDataListener <|.. CsvHistoryRecorder
    SimulationRunner --> MatchingEngine
    SimulationRunner --> AccountManager
    SimulationRunner --> MarketDataPublisher
```

## 3. 时序图

### 3.1 限价买单与挂单卖单成交（含部分成交与价格改善）

```mermaid
sequenceDiagram
    participant Agent as 买方 Agent
    participant Engine as MatchingEngine（持锁）
    participant Book as OrderBook
    participant Acc as Account
    participant Pub as MarketDataPublisher

    Agent->>Engine: submit(BUY AAPL 100 @152)
    Engine->>Book: bestOpposite(BUY) → 卖单 60 @150
    Engine->>Acc: reserve(orderId, perShare=152, qty=100)
    Acc-->>Engine: true（冻结 15200，可用现金 -15200）
    loop 价格可成交
        Engine->>Engine: qty = min(100, 60) = 60
        Engine->>Acc: settleBuy(orderId, price=150, 60)
        Note over Acc: 释放冻结 152×60=9120，实际成本 150×60=9000，<br/>差额 120 退回可用现金，持仓 +60
        Engine->>Acc: settleSell(卖方 orderId, 150, 60)
        Engine->>Book: setLastPrice(150) / addVolume(60)
        Engine->>Book: remove(卖单)（已全部成交）
    end
    Engine->>Book: post(剩余 40 @152)
    Engine-->>Agent: OrderResult(状态=PARTIALLY_FILLED, 成交 1 笔)
    Engine->>Pub: publishTrade / publishSnapshot（锁外）
    Pub->>Agent: onTrade / onMarketSnapshot
```

### 3.2 市价单扫簿与剩余撤销

```mermaid
sequenceDiagram
    participant Agent as 买方 Agent
    participant Engine as MatchingEngine
    participant Book as OrderBook
    participant Acc as Account

    Agent->>Engine: submit(BUY AAPL 250 @MARKET)
    Engine->>Book: bestOpposite(BUY) → 卖一 150
    Engine->>Acc: reserve(perShare=150, qty=250)
    Engine->>Book: 依次吃 100@150、100@151（可买量 = 冻结剩余 + 可用现金）
    Note over Engine: 每笔都按被动方价成交，冻结按 150 释放，<br/>151 成交的差额从可用现金补足
    Engine->>Engine: 剩余 50 无可成交对手
    Engine->>Acc: release(orderId)（退回 150×50）
    Engine->>Engine: order.markCancelled()
    Engine-->>Agent: OrderResult(状态=CANCELLED, 成交 200)
```

### 3.3 撤单

```mermaid
sequenceDiagram
    participant Agent as Agent/GUI
    participant Engine as MatchingEngine
    participant Book as OrderBook
    participant Acc as Account

    Agent->>Engine: cancel(orderId)
    Engine->>Book: findResting(orderId)
    Book-->>Engine: Order（未终结）
    Engine->>Book: remove(order)
    Engine->>Engine: order.markCancelled()
    Engine->>Acc: release(orderId)（退回剩余冻结资金/持仓）
    Engine-->>Agent: true
    Engine->>Engine: publishSnapshot（锁外）
```

### 3.4 LLM 决策周期

```mermaid
sequenceDiagram
    participant Timer as 调度线程池
    participant Agent as LlmTradingAgent
    participant Prompt as PromptBuilder
    participant Client as DeepSeekClient
    participant Parser as DecisionParser
    participant Engine as MatchingEngine

    Timer->>Agent: onTick()
    Agent->>Agent: 从行情缓存取一个标的（轮换）
    Agent->>Prompt: buildUserPrompt(行情, 账户, 深度)
    Agent->>Client: complete(system, user)
    Note over Client: 限流（≥1 次/秒）、超时、response_format=json_object
    Client-->>Agent: 原始 JSON 文本
    Agent->>Parser: parse(json)
    Parser-->>Agent: TradingDecision（非法输入退化为 HOLD）
    Agent->>Agent: 价格裁剪到最新价 ±5%；数量按可用资金/持仓裁剪
    Agent->>Engine: submit(OrderRequest)
    Engine-->>Agent: OrderResult（受理并成交 / 拒绝并附原因）
```

## 4. 线程模型（对应规格 4.4）

| 线程 | 职责 | 说明 |
| --- | --- | --- |
| 主线程 | 装配 `SimulationRunner`、启动 GUI | 不参与撮合 |
| 调度线程池（`ScheduledExecutorService`） | 按各自间隔调用 `agent.onTick()` | agent 真并发，直接冲击引擎的线程安全设计 |
| 权益标记任务 | 每秒 `AccountManager#markAllEquity` | 维护最大回撤曲线 |
| JavaFX 线程 | 渲染与交互 | 行情回调里只做 `Platform.runLater` |
| 收尾 | `closeAndRank`：停调度 → 收盘撤单 → 排行榜落盘 | 顺序固定，避免收盘后仍有下单 |

### 锁与并发约定

1. **单锁撮合**：校验、冻结、撮合、记账、改簿全在 `MatchingEngine.lock` 内完成，
   等价于规格要求的 “`submitOrder()` 必须 synchronized”，保证状态变更串行化。
2. **锁顺序固定**：引擎锁 → 账户锁。账户层不回调引擎，因此不存在反向加锁，不会死锁。
3. **锁外副作用**：行情广播、CSV 落盘在锁外执行，监听者慢 I/O 不会拖住撮合；
   监听者抛异常只计数不中断撮合（`MarketDataPublisher#getListenerErrorCount`）。
4. **每次决策只处理一个标的**：AI agent 每 tick 只对一个标的调一次 LLM，控制额度与并发。

## 5. 关键不变量（测试覆盖）

| 不变量 | 覆盖测试 |
| --- | --- |
| 成交价取被动方价格，买方获得价格改善 | `matchesAtRestingPrice` |
| 同价按时间优先、不同价按价格优先 | `samePriceRespectsTimePriority`、`betterPriceTakesPrecedence` |
| 撮合后订单簿不交叉（bestBid < bestAsk） | `concurrentSubmissionsKeepInvariants` |
| 资金守恒：现金 + 成本基础 = 期初 + 已实现盈亏 | `cashAndCostBasisAreConserved`、`concurrentSubmissionsKeepInvariants` |
| 撤单/收盘后冻结全部释放、可用现金恢复 | `cancelReleasesFrozenCash`、`closedMarketRejectsOrders` |
| 资金/持仓不足被拒绝且无副作用 | `buyRejectedWhenInsufficientCash`、`sellRejectedWhenInsufficientShares` |
| 多标的互不干扰 | `multiSymbolBooksAreIsolated` |
| 并发下无重复订单号、金额不为负 | `concurrentSubmissionsKeepInvariants` |
| 收益率不把初始持仓误算为收益 | `seededHoldingsDoNotInflateReturnRate` |
| 端到端：真起线程产生成交并落盘 | `SimulationIntegrationTest` |
