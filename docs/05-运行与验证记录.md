# 05 运行与验证记录

本文记录**真实执行过的验证**（命令 + 原始输出片段），用于证明系统确实可运行，而不是“代码写完了”。
每次重要改动后应更新本节末尾的“验证时间线”。

环境：Windows 11 / JDK 21.0.12 (Microsoft) / Maven 3.9.9（仓库自带 wrapper）/ git-bash。

## 1. 构建与测试

```bash
cd G:/项目/java-stock-auction
mvnw.cmd -B test        # 在 cmd / PowerShell / IntelliJ 中执行
# git-bash 中若 mvnw 报 classworlds 错误，用 shim：
"C:/Users/JXGU/DevTools/Runtimes/apache-maven-3.9.9/bin/mvn-msys" -B test
```

真实输出（节选）：

```
[INFO] Tests run: 9, Failures: 0, Errors: 0, Skipped: 0 -- in edu.cufe.auction.account.AccountTest
[INFO] Tests run: 6, Failures: 0, Errors: 0, Skipped: 0 -- in edu.cufe.auction.agent.ai.DecisionParserTest
[INFO] Tests run: 19, Failures: 0, Errors: 0, Skipped: 0 -- in edu.cufe.auction.engine.MatchingEngineTest
[INFO] Tests run: 1, Failures: 0, Errors: 0, Skipped: 0 -- in edu.cufe.auction.sim.SimulationIntegrationTest
[INFO] Tests run: 35, Failures: 0, Errors: 0, Skipped: 0
[INFO] BUILD SUCCESS
```

`SimulationIntegrationTest` 会真起线程跑 2 秒模拟（3 个做市 agent × 2 个标的），断言：
产生成交、`trades.csv` 有数据、`ranking.csv` 行数 = agent 数 + 1、收盘后订单簿清空、所有账户现金与可卖数量非负。

## 2. 控制台演示（真实输出节选）

```bash
mvnw.cmd exec:java -Dexec.mainClass=edu.cufe.auction.app.ConsoleApp -Dexec.args="12"
```

```
=== 多智能体连续竞价模拟（控制台版） ===
标的：{AAPL=175.50, GOOGL=141.80, TSLA=245.30}
运行时长：12 秒；LLM Key 未配置（AI 走规则回退）
[演示] 人类 broker 追价买入 AAPL 50 股 @ 177.26
AAPL   最新 175.88   涨跌   0.22%  量 249     买一 175.88   卖一 —
GOOGL  最新 142.05   涨跌   0.18%  量 67      买一 142.62   卖一 —
TSLA   最新 245.67   涨跌   0.15%  量 120     买一 246.48   卖一 —
  AAPL 买盘 175.88×38 | 卖盘 177.49×192
[演示] 人类 broker 挂低价买单 AAPL 50 股 @ 173.75
AAPL   最新 173.29   涨跌  -1.26%  量 337     买一 —        卖一 171.77
...
[SimulationRunner] 收盘：撤销挂单 29 笔，排行榜已写入 G:\项目\java-stock-auction\data\ranking.csv

=== 期末排行榜（按收益率） ===
  1. 噪音做市-1       权益    352819.57 收益率     0.60% 回撤    0.75% 胜率  79.17%
  2. 噪音做市-2       权益    352327.41 收益率     0.46% 回撤    1.31% 胜率  88.00%
  3. AI交易员-2      权益    111889.39 收益率     0.31% 回撤    0.74% 胜率  37.50%
  4. 人类Broker-A   权益    124830.50 收益率     0.15% 回撤    0.60% 胜率   0.00%
  5. 人类Broker-B   权益    124772.00 收益率     0.11% 回撤    0.36% 胜率   0.00%
  6. AI交易员-1      权益    111471.77 收益率    -0.07% 回撤    0.66% 胜率  33.33%
  7. AI交易员-3      权益    111277.36 收益率    -0.24% 回撤    0.84% 胜率   0.00%

=== agent 统计 ===
  人类Broker-A[human-1 类型=HUMAN 下单=3 被拒=0 成交=0]
  AI交易员-1[ai-1 类型=AI 下单=12 被拒=0 成交=187]
  噪音做市-1[noise-1 类型=NOISE 下单=26 被拒=0 成交=2045]
  噪音做市-2[noise-2 类型=NOISE 下单=29 被拒=0 成交=1886]
CSV 输出目录：G:\项目\java-stock-auction\data
```

**结论**：价格由订单碰撞产生（AAPL 从 175.50 走到 172.34，约 -1.8%），成交、撤单、收盘清算、
排行榜与 CSV 全部真实工作。

## 3. LLM 真实调用验证

```bash
KEY=$(tr -d '\r\n' < ~/api-key/deepseek/Token.txt)
LLM_API_KEY="$KEY" mvnw.cmd exec:java -Dexec.mainClass=edu.cufe.auction.app.ConsoleApp -Dexec.args="14"
```

真实输出（节选）：

```
运行时长：14 秒；LLM Key 已配置
[ai-3] LLM 决策 BUY 171.47 × 100（AAPL现价低于昨收且卖一171.47有承接，账户现金充足，轻仓买入摊低成本） -> 受理，成交 100 股
  AI交易员-1[ai-1 ...] LLM[调用=3 失败=0 观望=3]
  AI交易员-3[ai-3 类型=AI 下单=1 被拒=0 成交=100] LLM[调用=3 失败=0 观望=2]
```

**结论**：真实调用 `https://api.deepseek.com/v1/chat/completions` 成功，返回 JSON 被正确解析、
通过校验并真实成交；限流（1 次/秒）与失败回退路径均生效（`失败=0`）。密钥只经环境变量注入，未落库。

## 4. JavaFX 界面验证

```bash
mvnw.cmd javafx:run
```

真实输出（启动后由进程持有窗口，由超时信号结束）：

```
[AuctionUi] 界面已启动：标的 [AAPL, GOOGL, TSLA]，agent 7 个，数据目录 G:\项目\java-stock-auction\data
```

界面包含：盘口买卖盘深度表（各 5 档）、最新价/买一/卖一、最近成交、实时排行榜（权益/收益率/回撤/胜率）、
手动下单面板（限价/市价、方向、数量）。行情回调统一经 `Platform.runLater` 切回 JavaFX 线程。

> 说明：GUI 目前只做到“可运行 + 核心信息齐全”，价格走势图与 P&L 配色留到第 12 周的 GUI 阶段，
> 见 `docs/04` 第 2 节。

## 5. 本次开发中发现并修复的真实缺陷（含回归测试）

| # | 缺陷 | 现象 | 根因 | 修复与回归 |
| --- | --- | --- | --- | --- |
| 1 | agent 未订阅行情总线 | 模拟跑完 0 笔成交 | `SimulationRunner` 只把 CSV 记录器挂到 `MarketDataPublisher`，agent 收不到行情 → 永不下单 | 构造完成后统一订阅所有 `MarketDataListener` agent；由 `SimulationIntegrationTest` 守住 |
| 2 | 开盘无行情快照 | 市场静默不动 | 快照只在订单受理后推送，形成“没人下单→没行情→没人下单”死锁 | `start()` 先推一次开盘行情 |
| 3 | 收益率基准错误 | 做市 agent 凭空 +339% 收益 | 只按“初始资金”做分母，未计入注入的初始持仓成本 | 新增 `getInitialEquity()` 作为收益率基准；回归测试 `seededHoldingsDoNotInflateReturnRate` |

前两个缺陷都是**集成测试**（真起线程）才暴露的：单元测试全绿时市场依然一动不动。
这也是本项目坚持保留端到端集成测试的原因。

## 6. 验证时间线

| 日期 | 变更 | 验证 |
| --- | --- | --- |
| 2026-09-24 | Phase 1：引擎/账户/行情/agent/LLM/GUI/CSV + 文档 | `mvnw.cmd test` 35 项全绿；控制台演示产生成交与排行榜；`javafx:run` 界面启动；真实 DeepSeek 调用成交 100 股 |
